# Components 1-2 (benchmark, eval) are stdlib-only + numpy.
# Component 3 (training) additionally needs torch + transformers, even for
# the offline --model tiny smoke test. Developed/verified with:
# numpy 2.1.3, torch 2.10.0, transformers 5.2.0 on Python 3.13.
numpy>=1.24
torch>=2.1
transformers>=4.40
